[defaults]
inventory = inventory.ini
log-path = /var/log/ansible.log  
# ask_vault_pass = True
# vault_password_file = ~/.ansible/.mysql_vault
# forks = 4
private_key_file = /home/ec2-user/ansible
remote_user = ansible